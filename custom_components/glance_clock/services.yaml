send_timer:
  name: "Send Timer"
  description: "Send a timer scene to the Glance Clock. The timer will run on the device."
  fields:
    countdown:
      name: "Countdown (seconds)"
      description: >-
        Delay before the timer starts. NOT how long it counts -- that is the
        'duration' of each interval.
      required: false
      selector:
        number:
          min: 0
          max: 86400
          step: 1
    final_text:
      name: "Final Text (optional)"
      description: "Text to display when the timer finishes. Supports [icon:CODE] syntax."
      required: false
      selector:
        text:
    intervals:
      name: "Intervals (optional)"
      description: |
        The stages of the timer, and the part that makes it a timer at all. At
        least one is required: a call without intervals reaches the clock and
        displays nothing. Each interval can have:
          - text: string (supports [icon:CODE])
          - duration: seconds (int) -- how long this stage runs
          - countdown: seconds (int, optional)
        For a plain single countdown, give one interval with a duration.
      required: false
      selector:
        object:
          label_field: text
          description_field: duration
          multiple: true
          fields:
            text:
              label: "Text"
              selector:
                text:
            duration:
              label: "Duration (seconds)"
              selector:
                number:
                  min: 1
                  max: 86400
                  step: 1
            countdown:
              label: "Countdown (seconds, optional)"
              selector:
                number:
                  min: 0
                  max: 86400
                  step: 1

update_display_settings:
  name: "Update Display Settings"
  description: "Update display settings on the Glance Clock"
  fields:
    nightModeEnabled:
      name: "Night Mode Enabled"
      description: "Enable automatic night mode"
      default: true
      selector:
        boolean:
    pointsAlwaysEnabled:
      name: "Points Always Enabled"
      description: "Always show time separator points"
      default: false
      selector:
        boolean:
    displayBrightness:
      name: "Display Brightness"
      description: "Screen brightness level (0-255, 0=auto). Note: Use the light entity for easier brightness control"
      default: 128
      selector:
        number:
          min: 0
          max: 255
          step: 1
    timeModeEnable:
      name: "Time Mode Enable"
      description: "Enable time display on the clock"
      default: true
      selector:
        boolean:
    timeFormat12:
      name: "12-Hour Format"
      description: "Use 12-hour format instead of 24-hour"
      default: false
      selector:
        boolean:
    dateFormat:
      name: "Date Format"
      description: "Date display format (0=Disabled, 1=24 Jan, 2=24 Tue, 3=Jan 24, 4=Tue 24)"
      default: 0
      selector:
        number:
          min: 0
          max: 4
          step: 1

send_notice:
  name: "Send Notice"
  description: "Send a notification notice to the Glance Clock"
  fields:
    text:
      name: "Text"
      description: |
        Message text to display. To insert an icon, use [icon:CODE] in your message (e.g., "Wake up! [icon:123]").

        See the full list of available icons: https://github.com/Snille/glance_clock_ha/blob/master/ICONS.md
      required: true
      selector:
        text:
    animation:
      name: "Animation"
      description: "Visual animation effect"
      default: "pulse"
      selector:
        select:
          options:
            - label: "None"
              value: "none"
            - label: "Pulse"
              value: "pulse"
            - label: "Wave"
              value: "wave"
            - label: "Fire"
              value: "fire"
            - label: "Wheel"
              value: "wheel"
            - label: "Flower"
              value: "flower"
            - label: "Flower2"
              value: "flower2"
            - label: "Fan"
              value: "fan"
            - label: "Sun"
              value: "sun"
            - label: "Thunderstorm"
              value: "thunderstorm"
            - label: "Cloud"
              value: "cloud"
            - label: "Weather: Clear"
              value: "weather_clear"
            - label: "Weather: Cloudy"
              value: "weather_cloudy"
            - label: "Weather: Fog"
              value: "weather_fog"
            - label: "Weather: Light Rain"
              value: "weather_light_rain"
            - label: "Weather: Rain"
              value: "weather_rain"
            - label: "Weather: Thunderstorm"
              value: "weather_thunderstorm"
            - label: "Weather: Snow"
              value: "weather_snow"
            - label: "Weather: Hail"
              value: "weather_hail"
            - label: "Weather: Wind"
              value: "weather_wind"
            - label: "Weather: Tornado"
              value: "weather_tornado"
            - label: "Weather: Hurricane"
              value: "weather_hurricane"
            - label: "Weather: Snow Thunderstorm"
              value: "weather_snow_thunderstorm"
    sound:
      name: "Sound"
      description: "Notification sound"
      default: "none"
      selector:
        select:
          options:
            - label: "None"
              value: "none"
            - label: "Waves"
              value: "waves"
            - label: "Rise"
              value: "rise"
            - label: "Charging"
              value: "charging"
            - label: "Steps"
              value: "steps"
            - label: "Radar"
              value: "radar"
            - label: "Bells"
              value: "bells"
            - label: "Bye"
              value: "bye"
            - label: "Hello"
              value: "hello"
            - label: "Flowers"
              value: "flowers"
            - label: "Circles"
              value: "circles"
            - label: "Complete"
              value: "complete"
            - label: "Popcorn"
              value: "popcorn"
            - label: "Break"
              value: "break"
            - label: "Opening"
              value: "opening"
            - label: "High"
              value: "high"
            - label: "Shine"
              value: "shine"
            - label: "Extension"
              value: "extension"
    color:
      name: "Color"
      description: "Display color"
      default: "white"
      selector:
        select:
          options:
            - label: "Black"
              value: "black"
            - label: "Dark Golden Rod"
              value: "dark_golden_rod"
            - label: "Dark Orange"
              value: "dark_orange"
            - label: "Olive"
              value: "olive"
            - label: "Orange Red"
              value: "orange_red"
            - label: "Red"
              value: "red"
            - label: "Maroon"
              value: "maroon"
            - label: "Dark Magenta"
              value: "dark_magenta"
            - label: "Medium Violet Red"
              value: "medium_violet_red"
            - label: "Brown"
              value: "brown"
            - label: "Indigo"
              value: "indigo"
            - label: "Blue Violet"
              value: "blue_violet"
            - label: "White"
              value: "white"
            - label: "Light Slate Blue"
              value: "light_slate_blue"
            - label: "Royal Blue"
              value: "royal_blue"
            - label: "Blue"
              value: "blue"
            - label: "Cornflower Blue"
              value: "cornflower_blue"
            - label: "Sky Blue"
              value: "sky_blue"
            - label: "Turquoise"
              value: "turquoise"
            - label: "Aqua"
              value: "aqua"
            - label: "Medium Spring Green"
              value: "medium_spring_green"
            - label: "Lime Green"
              value: "lime_green"
            - label: "Dark Green"
              value: "dark_green"
            - label: "Lime"
              value: "lime"
            - label: "Lawn Green"
              value: "lawn_green"
    priority:
      name: "Priority"
      description: "Notification priority level"
      default: "medium"
      selector:
        select:
          options:
            - label: "Low"
              value: "low"
            - label: "Medium"
              value: "medium"
            - label: "High"
              value: "high"
            - label: "Highest"
              value: "highest"
            - label: "Critical"
              value: "critical"
    text_modifier:
      name: "Text Effect"
      description: "Text display effect"
      default: "none"
      selector:
        select:
          options:
            - label: "None"
              value: "none"
            - label: "Repeat"
              value: "repeat"
            - label: "Rapid"
              value: "rapid"
            - label: "Delay"
              value: "delay"

read_current_settings:
  name: "Read Current Settings"
  description: "Read current settings from the Glance Clock device"

refresh_entities:
  name: "Refresh Entities"
  description: "Refresh all entity states for the Glance Clock"

send_forecast:
  name: "Send Weather Forecast"
  # Writes to scene slot 1. Remove it again with clear_leds and slot 1 -- a
  # scene stays in its slot and replays until something clears it.
  description: "Send 24-hour weather forecast data to the Glance Clock with color gradient support"
  fields:
    weather_entity:
      name: "Weather Entity"
      description: "The weather entity to get hourly forecast data from"
      required: true
      selector:
        entity:
          domain: weather
    max_color:
      name: "Maximum Temperature Color"
      description: "Color for maximum temperature display"
      default: "#FF0000"
      selector:
        color_rgb:
    min_color:
      name: "Minimum Temperature Color"
      description: "Color for minimum temperature display"
      default: "#0000FF"
      selector:
        color_rgb:
    min_value:
      name: "Minimum Value Range"
      description: "Temperature value for the minimum color (0-40°C range). If not set, uses actual forecast minimum."
      selector:
        number:
          min: -50
          max: 50
          step: 1
    max_value:
      name: "Maximum Value Range"
      description: "Temperature value for the maximum color (0-40°C range). If not set, uses actual forecast maximum."
      selector:
        number:
          min: -50
          max: 50
          step: 1
    template:
      name: "Display Template"
      description: "Optional display template data (advanced users). If not provided, uses default thermometer icon + value + °C"
      selector:
        text:

set_dnd_schedule:
  name: Set DND schedule
  description: >-
    Set the recurring Do Not Disturb window stored on the clock. This is the
    schedule the clock applies itself, separate from the permanent DND flag.
  fields:
    from_hour:
      name: From hour
      description: Hour the quiet period starts, 0-23.
      example: 21
      selector:
        number:
          min: 0
          max: 23
          mode: box
    till_hour:
      name: Till hour
      description: Hour the quiet period ends, 0-23. May be earlier than the start to span midnight.
      example: 7
      selector:
        number:
          min: 0
          max: 23
          mode: box
    recurring:
      name: Recurring
      description: Whether the window repeats every day.
      default: true
      selector:
        boolean:

read_dnd_schedule:
  name: Read DND schedule
  description: >-
    Read the Do Not Disturb window currently stored on the clock and write it
    to the Home Assistant log.

set_leds:
  name: Set LEDs
  description: >-
    Light areas of the clock's four LED rings. Each ring has 48 pixels; pixel 0 is at
    twelve o'clock and numbering runs clockwise. Ring 0 is the outermost.
  fields:
    segments:
      name: Segments
      description: >-
        List of areas to light. Each needs 'start' (0-47) and 'color' (a colour name),
        and may set 'length' (pixels, default 1), 'ring' (0-3, default 0) and
        'rings_tall' (default 1).
      required: true
      example: |-
        - start: 0
          length: 12
          ring: 0
          color: red
        - start: 24
          length: 6
          ring: 1
          color: sky_blue
      selector:
        object:
    mode:
      name: Display mode
      description: >-
        How the scene shares the display. 'watchface' shows it together with the digital
        time, 'exclusive' hides the digital clockface, 'ring_and_text' alternates with it.
      default: watchface
      selector:
        select:
          mode: dropdown
          options:
            - watchface
            - exclusive
            - ring_and_text
    slot:
      name: Scene slot
      description: Which scene slot to store this in. Reusing a slot replaces its contents.
      default: 0
      selector:
        number:
          min: 0
          max: 7
          mode: box
    life_time:
      name: Lifetime
      description: >-
        Frames at 50 FPS before the scene stops animating. A fill stays on screen
        afterwards, so this is not how long it is visible.
      default: 50
      selector:
        number:
          min: 1
          max: 3000
          mode: box

clear_leds:
  name: Clear LEDs
  description: Remove a scene previously sent with Set LEDs.
  fields:
    slot:
      name: Scene slot
      description: The slot to clear.
      default: 0
      selector:
        number:
          min: 0
          max: 7
          mode: box

set_animation:
  name: Set animation
  description: >-
    Run one of the animations built into the clock's firmware on the LED rings. They
    are single-colour patterns tinted by the colour you pick.
  fields:
    animation:
      name: Animation
      description: >-
        'sweep' fills the area from its start pixel, pauses and repeats; its speed sign
        sets the direction. The rest are firmware effects.
      default: fire
      selector:
        select:
          mode: dropdown
          options:
            - sweep
            - fire
            - wheel
            - flower
            - flower2
            - fan
            - sun
            - thunderstorm
            - cloud
    color:
      name: Colour
      description: Colour name from the clock's palette. Black renders nothing.
      default: white
      selector:
        text:
    speed:
      name: Speed
      description: >-
        0-10 for the firmware animations. 'sweep' takes -10 to 10, where a negative
        value runs anticlockwise.
      default: 3
      selector:
        number:
          min: -10
          max: 10
          mode: slider
    start:
      name: Start pixel
      description: 0-47, clockwise from twelve o'clock.
      default: 0
      selector:
        number:
          min: 0
          max: 47
          mode: box
    length:
      name: Length
      description: >-
        Pixels to cover. The firmware animations only render multiples of 8 and round
        anything else down.
      default: 48
      selector:
        number:
          min: 1
          max: 48
          mode: box
    ring:
      name: Ring
      description: 0-3, counted from the outer edge.
      default: 0
      selector:
        number:
          min: 0
          max: 3
          mode: box
    rings_tall:
      name: Rings tall
      description: How many rings the animation covers.
      default: 4
      selector:
        number:
          min: 1
          max: 4
          mode: box
    mode:
      name: Display mode
      default: watchface
      selector:
        select:
          mode: dropdown
          options:
            - watchface
            - exclusive
            - ring_and_text
    slot:
      name: Scene slot
      default: 0
      selector:
        number:
          min: 0
          max: 7
          mode: box
    life_time:
      name: Lifetime
      description: Frames at 50 FPS that the animation runs for.
      default: 2500
      selector:
        number:
          min: 1
          max: 30000
          mode: box

set_scene:
  name: Set scene
  description: >-
    Send a timed sequence of fills that the clock plays itself at 50 frames per second.
    This is how to animate: one call uploads the whole timeline, and the clock runs it
    at its own frame rate. Sending fills one at a time cannot animate -- every call is a
    Bluetooth round trip, so the timing is whatever the radio happened to do.
  fields:
    steps:
      name: Steps
      description: >-
        List of steps. Every step takes 'frames' or 'seconds' for how long it runs, and
        optionally 'at' to place it on the timeline; without 'at' a step follows on from
        the previous one. A step's 'type' is one of: 'fill' (default, takes 'segments'),
        'effect' (pulse, wave or light_flash, layered onto areas already drawn in this
        scene), 'text', 'sound', or 'weather' (snow, rain or fog as a particle effect).
        Anything drawn stays on screen after its step ends, so paint black over what
        should disappear.
      required: true
      example: |-
        - seconds: 0.5
          segments:
            - { start: 0, length: 6, ring: 0, rings_tall: 4, color: red }
        - seconds: 0.5
          segments:
            - { start: 0, length: 6, ring: 0, rings_tall: 4, color: black }
            - { start: 6, length: 6, ring: 0, rings_tall: 4, color: red }
      selector:
        object:
    default_frames:
      name: Default frames
      description: How long a step runs when it gives neither frames nor seconds.
      default: 25
      selector:
        number:
          min: 1
          max: 3000
          mode: box
    mode:
      name: Display mode
      default: watchface
      selector:
        select:
          mode: dropdown
          options:
            - watchface
            - exclusive
            - ring_and_text
    slot:
      name: Scene slot
      default: 0
      selector:
        number:
          min: 0
          max: 7
          mode: box

send_command:
  name: Send command
  description: >-
    Send a raw command frame to the clock. This is the escape hatch for parts of the
    firmware the integration does not model yet, and for driving the clock from
    Node-RED. A frame is a command byte, up to three modifier bytes, and an optional
    payload; missing modifiers are padded with zeroes. Useful commands include 30
    (stop scene playback), 31 (start scene playback), 35 (update and refresh) and 61
    and 60 (start and stop the brightness scene). Commands 42 and 50 are blocked
    because they unpair the clock or wipe user data, and neither can be undone
    without the manufacturer's servers.
  fields:
    command:
      name: Command
      description: The command byte.
      required: true
      example: 31
      selector:
        number:
          min: 0
          max: 255
          mode: box
    modifiers:
      name: Modifiers
      description: Up to three modifier bytes. Omitted ones are sent as zero.
      required: false
      example: "[0, 0, 0]"
      selector:
        object:
    payload:
      name: Payload
      description: Optional payload, as a hex string such as "20 01" or a list of bytes.
      required: false
      example: "2001"
      selector:
        text:

read_characteristic:
  name: Read characteristic
  description: >-
    Read one of the clock's GATT characteristics and return its bytes. Returns a
    response rather than logging, so the result comes back to the caller.
  fields:
    characteristic:
      name: Characteristic
      description: >-
        One of settings, scene_data, scene_state, or a full UUID. Use "list" to
        enumerate every service and characteristic the clock exposes, with the
        properties each one supports.
      required: false
      default: settings
      example: scene_state
      selector:
        text:

stop_timer:
  name: Stop timer
  description: >-
    Cancel a timer the clock is currently running. Send Timer starts one and the
    clock counts it down by itself, so without this there was no way to call it
    off short of clearing the display.

stop_alarm:
  name: Stop alarm
  description: Silence an alarm the clock is sounding.

stop_scenes:
  name: Stop scene playback
  description: >-
    Stop the scene engine. Scenes stay in their slots but nothing is drawn from
    them until playback resumes. Note that the stop does not hold: the clock
    restarts playback by itself after about a second, which was measured on
    hardware rather than intended.

next_scene:
  name: Next scene
  description: >-
    Show the next filled slot, and restart its fifteen-second turn. With several
    slots filled the clock rotates between them on its own; this steps that
    rotation forward. With one slot filled the next scene is the same scene,
    which is why a scene write sends this to make the new scene appear at once.
    Sending it by hand delays the next scene write until the following tick.

send_rain_forecast:
  name: Send rain forecast
  description: >-
    Draw the next 24 hours of precipitation around the dial, using the clock's own
    forecast face. Needs a weather entity with an hourly forecast. Goes to scene
    slot 2, so it can stand alongside the temperature forecast in slot 1.
  fields:
    weather_entity:
      name: Weather entity
      description: A weather entity that provides an hourly forecast.
      required: true
      selector:
        entity:
          domain: weather
    max_value:
      name: Top of the scale
      description: >-
        Millimetres per hour drawn at full height. This is a scale, not a filter --
        heavier rain is still drawn, just not taller. The default of 2 mm/h is
        moderate rain, which keeps ordinary drizzle visible.
      default: 2
      selector:
        number:
          min: 0.1
          max: 50
          step: 0.1
          unit_of_measurement: mm/h
          mode: box
    slot:
      name: Scene slot
      description: Which slot to draw in. Slot 1 belongs to the temperature forecast.
      default: 2
      selector:
        number:
          min: 0
          max: 7
          mode: box
    min_color:
      name: Dry colour
      description: Colour for an hour with no rain, as a hex value.
      default: "0x000010"
      selector:
        text:
    max_color:
      name: Wettest colour
      description: Colour for an hour at the top of the scale, as a hex value.
      default: "0x00BFFF"
      selector:
        text:

send_daylight_forecast:
  name: Send daylight forecast
  description: >-
    Draw which of the next 24 hours are daylight. Needs no weather entity -- Home
    Assistant already knows where the sun is, so this is arithmetic rather than a
    forecast. An hour counts as lit if its midpoint falls between sunrise and
    sunset.
  fields:
    slot:
      name: Scene slot
      description: Which slot to draw in. Slot 1 belongs to the temperature forecast.
      default: 2
      selector:
        number:
          min: 0
          max: 7
          mode: box
    min_color:
      name: Night colour
      description: Colour for an hour of darkness, as a hex value.
      default: "0x000010"
      selector:
        text:
    max_color:
      name: Daylight colour
      description: Colour for an hour of daylight, as a hex value.
      default: "0xFFD700"
      selector:
        text:
